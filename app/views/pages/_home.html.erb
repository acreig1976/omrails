<% if signed_in? %>
      .
      .
      .
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/stats' %>
      </section>
      <section>
        <%= render 'shared/micropost_form' %>
      </section>
      .
      .
      .
<% else %>
  .
  .
  .
<% end %>

<% unless user_signed_in? %>
	<h1>Welcome to DISTRICT</h1>

	<p>
		You have found the homepage of
		<%= link_to "DISTRICT", "http://dstrct.org"%>
	</p>
	<p>
		<%= link_to "Sign Up Now!", new_user_registration_path %>
	</p>
<% end %>